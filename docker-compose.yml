version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chess-frontend-dev
    ports:
      - "8081:8081"
    environment:
      - NODE_ENV=production
